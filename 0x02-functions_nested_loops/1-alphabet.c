#include "main.h"
/**
 * print _alphabet - prints the alphabet in lowercase.
 *
 * Return: Always 0 (success)
 *
 */
void print_alphabet(void)
{
print_alphabet(i);
for (i=97; i <= 122; i++)
{
_putchar(i);
}
_putchar('\n'):
_putchar('\n');
}
